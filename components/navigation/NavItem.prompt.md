One-line: A single nav link used in the top bar and footer — 14px semibold.

```jsx
<NavItem active>Home</NavItem>
<NavItem>Our services</NavItem>
<NavItem onDark>Contact us</NavItem>
```

Pass `onDark` inside the black footer. `active` colors the current page navy.
