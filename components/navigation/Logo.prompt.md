One-line: The BCA Partners logo — the real image mark on light backgrounds, or the blue "BCA Partners" wordmark in type on dark (the footer treatment).

```jsx
<Logo src="assets/logo-bca.png" />
<Logo variant="wordmark" />
```

The source has no dark-background logo mark, so the wordmark renders in type by design.
