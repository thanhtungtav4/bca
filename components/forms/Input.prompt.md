One-line: The contact-form field — a 14px semibold label above a 4px-radius bordered input (or textarea).

```jsx
<Input label="Full name" placeholder="Your name" />
<Input label="Message" multiline rows={5} placeholder="How can we help?" />
```

Border is `--bca-border`; the single brand 4px radius applies.
