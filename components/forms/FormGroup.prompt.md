One-line: The kit's "form-group" — a labelled field/textarea. Identical to `Input` (the source calls it form-group).

```jsx
<FormGroup label="Email" type="email" placeholder="you@company.com" />
```
