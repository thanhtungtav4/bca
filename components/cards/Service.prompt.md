One-line: The Figma "Service" tile — a photo with a solid frosted caption bar and a 22px bold service name (e.g. "Mergers & Acquisitions").

```jsx
<Service image="assets/service-strategy.jpg" title="Strategy" />
```

Sibling to `ServiceCard` — `Service` matches the exact source symbol (60px bar, 22px title); `ServiceCard` is the homepage-grid variant (61px bar, 24px title).
