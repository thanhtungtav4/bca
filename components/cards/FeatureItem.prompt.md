One-line: The source's atomic rounded photo block ("_Feature Item") — a 4px-radius image on a grey placeholder fill.

```jsx
<FeatureItem image="assets/project-dragonfruit.jpg" height={256} />
```

Compose captions/overlays as children. `FeatureCard` layers a glass panel on top of this.
