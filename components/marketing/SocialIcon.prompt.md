One-line: A circular social-media chip — the translucent-white LinkedIn button from the footer.

```jsx
<SocialIcon name="SocialIconSocialLinkedInStyleWhiteType" />
```

`name` is any glyph from the materialized set (`assets/icons/Icon.d.ts` — 64 social variants incl. LinkedIn/Facebook/Twitter/YouTube).
