One-line: An app-store download badge (Figma "Store badge"/"_App badges"). Supply the official App Store / Google Play PNG via `src` — the trademarked marks are never redrawn.

```jsx
<StoreBadge store="appstore" src="assets/appstore.png" />
```
