One-line: The kit's "Heading project" — a navy 40px bold page/section heading (e.g. "Contact Us"). `SectionHeading` wraps this with an optional lead paragraph.

```jsx
<HeadingProject>Our Services</HeadingProject>
```
